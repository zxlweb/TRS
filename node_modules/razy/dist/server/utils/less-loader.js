/**
 * @fileOverview read css/less file in server
 * @author Max
 */
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var fs = require("fs");
var path = require("path");
function importFile(filePath, dirname) {
    var filePathObj = path.parse(filePath);
    var cssFilePath = path.join(dirname, filePathObj.dir, filePathObj.name + '.css');
    try {
        var content = fs.readFileSync(cssFilePath, 'utf-8');
        return content;
    }
    catch (e) {
        console.error(e);
    }
    return '';
}
exports.importFile = importFile;
