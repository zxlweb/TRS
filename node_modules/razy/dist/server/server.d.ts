/// <reference types="express" />
/**
 * @fileOverview 服务器入口
 * @author Max
 **/
import * as express from 'express';
import './config';
import { ParamsInterface } from '../index';
export declare function start(params: ParamsInterface, interceptor?: (app: express.Express) => void): void;
