/**
 * @fileOverview utils
 * @author Max
 */
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var cloneDeep = require("lodash.clonedeep");
function exportConfigToGlobalConst(config) {
    var result = {};
    var read = function (key, obj) {
        if (typeof obj === 'object') {
            for (var i in obj) {
                var item = obj[i];
                var newKey = cloneDeep(key);
                newKey.push(i);
                read(newKey, item);
            }
        }
        else {
            var keyStr = "__" + key.join('_') + "__";
            if (keyStr.indexOf('@') !== -1)
                return; // escape config containing '@'
            result[keyStr] = obj;
        }
    };
    read([], config);
    return result;
}
exports.exportConfigToGlobalConst = exportConfigToGlobalConst;
