/**
 * @fileOverview utils
 * @author Max
 */
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function injectConfVariable(conf, prefix) {
    var processedPrefix = prefix ? prefix + '_' : '';
    for (var i in conf) {
        var key = i.charAt(0) === '@' ? i.substr(1) : i; // remove the starting '@' from keys
        if (typeof conf[i] === 'object') {
            injectConfVariable(conf[i], "" + processedPrefix + key);
        }
        else {
            global["__" + processedPrefix + key + "__"] = conf[i];
        }
    }
}
exports.injectConfVariable = injectConfVariable;
