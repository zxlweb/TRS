import REQUEST from './request';
import { RequestParam, DataFlagResolver } from './request';
export declare class HTTP_PARENT extends REQUEST {
    private method;
    constructor(params: RequestParam, protocol: string, method: any, dataFlagResolver?: DataFlagResolver);
    protected getPromise(url: string, raw?: boolean): Promise<{}>;
    protected postPromise(url: string, content: any, options?: any, raw?: boolean): Promise<{}>;
    private dataHandler(url, res, resolve, reject, raw?);
}
export declare class HTTP extends HTTP_PARENT {
    constructor(params: any, dataFlagResolver?: DataFlagResolver);
}
export declare class HTTPS extends HTTP_PARENT {
    constructor(params: any, dataFlagResolver?: DataFlagResolver);
}
export declare const initHTTP: (params: RequestParam, dataFlagResolver?: DataFlagResolver) => HTTP;
export declare const initHTTPS: (params: RequestParam, dataFlagResolver?: DataFlagResolver) => HTTPS;
