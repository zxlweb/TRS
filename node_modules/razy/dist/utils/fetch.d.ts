import REQUEST from './request';
import { RequestParam, DataFlagResolver } from './request';
export declare class Fetch extends REQUEST {
    private fetch;
    constructor(params: RequestParam, dataFlagResolver?: DataFlagResolver);
    protected getPromise(url: string, raw?: boolean): Promise<{}>;
    protected postPromise(url: string, content: any, options?: any, raw?: boolean): Promise<{}>;
    private _handleData(url, data, resolve, reject, raw?);
}
export declare const initFetch: (params: RequestParam, dataFlagResolver?: DataFlagResolver) => Fetch;
